package com.example.openair.ui.theme

import androidx.compose.ui.graphics.Color

// ── Paper palette ────────────────────────────────────────────────────────────
val PaperWhite  = Color(0xFFF5F0E8)   // warm off-white background
val PaperCream  = Color(0xFFEDE8D8)   // ruled-line tint / section bg
val PaperDeep   = Color(0xFFD4CDB8)   // pressed / chip background

// ── Ink palette ───────────────────────────────────────────────────────────────
val InkBlack    = Color(0xFF1A1A1A)   // borders, shadows, primary text
val InkFaded    = Color(0xFF4A4A4A)   // secondary text
val InkLight    = Color(0xFF9A9485)   // disabled / placeholder

// ── Ocean palette ─────────────────────────────────────────────────────────────
val OceanDeep   = Color(0xFF1B4F72)   // receiver-on, primary CTA
val OceanMid    = Color(0xFF2980B9)   // connected-device highlight
val OceanLight  = Color(0xFFAED6F1)   // scan ring, subtle accent

// ── Marker accents ────────────────────────────────────────────────────────────
val MarkerRed   = Color(0xFFC0392B)   // error / disconnect
val MarkerGreen = Color(0xFF1E8449)   // success / sending complete
val MarkerAmber = Color(0xFFD4AC0D)   // warning / partial